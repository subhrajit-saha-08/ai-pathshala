# Gemini API Key for LLM-as-a-judge (Required)
GEMINI_API_KEY="your_api_key_here"

# Server Configuration
HOST="0.0.0.0"
PORT=8000

# Security (Comma-separated list of allowed origins)
CORS_ORIGINS="http://localhost:3000"
